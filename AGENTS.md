# 项目协作规则

请默认使用简体中文与用户沟通，并优先使用中文编写 README、设计文档、任务说明、变更说明等项目文档。代码命名仍按英文和项目规范执行；技术术语可以保留英文，但必要时请补充中文解释。

## Autoresearch 进化流程

当用户要求“进化”“autoresearch”“自动优化”时，不要直接线性修改唯一方案。必须按 `docs/AUTORESEARCH.md` 执行：

1. 先明确本轮适应度目标和评分权重。
2. 每轮提出多个候选变体。
3. 对候选进行实现、验证、评分和淘汰。
4. 只把胜出变体合入主工作区。
5. 每轮留下报告，记录候选、分数、失败原因和下一轮方向。

默认验证命令：

```powershell
.\scripts\eval.ps1
```

